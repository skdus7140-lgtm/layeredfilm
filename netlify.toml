[build]
  command = "npm run build"
  publish = "dist"

# Netlify configuration for Layered Film Portfolio
[build.environment]
  NPM_FLAGS = "--legacy-peer-deps"
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
